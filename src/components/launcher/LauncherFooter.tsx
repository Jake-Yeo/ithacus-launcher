import { ArrowIcon } from '../icons/ArrowIcon'

export function LauncherFooter() {
  return <footer className="mt-8 flex items-center justify-between gap-4 text-micro font-medium text-muted"><span>Tailnet only</span><span className="flex items-center gap-1.5">Private by design <ArrowIcon className="size-3.5" /></span></footer>
}
